# 葳格高中實習課程 - 材料採購統整系統

## 網站連結
https://johnnywang885-gif.github.io/material-purchase-system/

## 功能說明

### 1. 各組輸入
- 各組填寫日期、星期、班級、組別、課程名稱、產品名稱
- 新增多筆材料清單（品名、規格、單位、數量、備註）
- 儲存後自動同步至 Firebase 雲端，支援跨裝置即時同步

### 2. 跨裝置即時同步
- 資料儲存於 Firebase Realtime Database
- 各裝置開啟同一網址即可同步資料
- 支援 Room ID 分區：`?room=班級名稱`（預設為 `wgcs`）
- 離線時自動降級至 localStorage，連線後恢復同步
- 同步狀態指示燈：🟢 已連線 / 🔴 離線 / 🟡 同步中

### 3. 快速匯入（文字自動解析）
- 將圖片中的需求文字複製貼上，系統自動辨識：
  - 組別（如：第五組）
  - 日期（如：3/16、3月16日）
  - 星期（如：(一)）
  - 產品名稱（材料清單前的短行文字）
  - 材料清單（品名 + 數量 + 單位，如：砂糖 24g）
- 支援「解析並帶入表單」或「一鍵解析並儲存」

### 3. 彙整總表
- 依日期分組顯示
- 相同材料自動合併加總數量
- 顯示需求組別來源

### 4. 列印採購單
- 產生正式「崴格高級中學實習課程材料採購單」格式
- 跨日期彙整所有材料
- 可直接列印或匯出

## 使用方式

### 直接使用
1. 雙擊 `index.html` 用瀏覽器開啟即可
2. 無需安裝任何軟體
3. 資料自動同步至 Firebase 雲端

### 跨裝置同步
1. 所有裝置開啟同一網址即可共享資料
2. 使用 `?room=班級名稱` 可建立獨立空間（如：`?room=餐二甲`）
3. 各 Room 資料互相獨立

### 部署到 GitHub Pages
1. 建立 GitHub Repository
2. 將 `index.html` 推送到 `master` 分支
3. 建立 `gh-pages` 分支並啟用 GitHub Pages
4. 網址格式：`https://[username].github.io/[repo-name]/`

## 技術架構

- **前端**：HTML + CSS + JavaScript（單一檔案）
- **資料儲存**：Firebase Realtime Database（雲端即時同步）
- **離線備援**：localStorage（離線時自動降級）
- **部署**：GitHub Pages
- **無後端依賴**：純前端 + Firebase SDK

## 檔案結構

```
├── index.html          # 主程式（含 HTML/CSS/JS）
├── README.md           # 說明文件
├── 各組材料需求表.jpg    # 參考圖片
└── 材料採購單彙整表.jpg  # 參考圖片
```

## 材料單位支援

g、kg、個、顆、條、瓶、包、盒、袋、罐、小瓶、小粒、粒、ml、L、克、公斤、片、塊、支、把、束、串、杯、碗、匙、勺、大匙、小匙、根、枝、枚、張、頁、組、套、式、批、份、台、隻、尾、羽、頭、對、雙、件、兩、錢、斤、箱、籃、桶

## 注意事項

- 資料同步至 Firebase Realtime Database，支援跨裝置即時同步
- 離線時資料暫存於 localStorage，連線後自動同步
- Firebase 免費方案支援同時 100 人連線，足夠班級使用
- 建議使用 Chrome、Edge 或 Safari 瀏覽器
